version: "3.9"

services:
  fastapi-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi_app
    ports:
      - "8000:8000"
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
    driver: local
